# Terms to never auto-link (pass 1/2)

# Generic recurring section headings shared by most articles (per
# vaults/.article-style.md's standing structure) — auto-linking their bare
# words produces misleading first-occurrence links to an arbitrary other
# article, not a genuine cross-reference.
Grounding
Where this leads
Synthesis
Syntax
The derivation
Elimination and computation
What breaks without it
